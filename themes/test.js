var pico = {
    asdasd: 123,

}

const hola = true;
let test = 5 + 5;


/**
 * GET /ColaboradorUsersGls
 * @description  lista de Colaboradores y su usuario GLS
 * @summary  lista de Colaboradores y su usuario GLS
 * @tags ColaboradorUsersGls - Metodos para listar la asociacion entre colaboradores y su usuario en GLS
 * @param {number} id.query - buscar por ID (opcional)
 * @param {number} rutColaborador.query - buscar por tipo de absentismo (opcional)
 * @param {string} glsUserName.query - buscar por usuario GLS (opcional)
 * @return {object} 200 - lista de areas que se encuentran activas - application/json
 * @return {object} 404 - no encontrado - application/json
 * @return {object} 500 - error no controlado - application/json
 */